export default function DecisionGateBadge({ gate }) {
  return (
    <span className="inline-block rounded-full text-xs font-medium bg-amber-100 text-amber-800 px-2 py-0.5">
      Gate {gate}
    </span>
  );
}
